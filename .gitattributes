# Normalise line endings so the repo is identical across Windows/macOS/Linux
# and diffs stay clean regardless of contributor platform.
* text=auto eol=lf

# Binary assets must never be munged.
*.png binary
*.jpg binary
*.gif binary
*.woff binary
*.woff2 binary
